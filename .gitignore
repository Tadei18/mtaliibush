# Dependencies & build output
node_modules/
.astro/
dist/
.output/
.vercel/
.netlify/
# Env & logs
.env
.env.*
*.log
# Real SMTP credentials for the enquiry mail handler — upload manually to the
# server, never commit. (mail-config.sample.php IS committed as the template.)
site/public/mail-config.php
# OS junk
.DS_Store
Thumbs.db
# Raw source video — too big for GitHub (keep local / use a CDN in prod)
Reels/
# Temp extraction folders from the zip-wrapped logo & booklet
_logo/
_booklet/
# Claude Code harness state
.claude/
